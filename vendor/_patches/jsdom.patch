diff --git a/lib/jsdom/living/attributes/NamedNodeMap-impl.js b/lib/jsdom/living/attributes/NamedNodeMap-impl.js
index c049973a..795587b7 100644
--- a/lib/jsdom/living/attributes/NamedNodeMap-impl.js
+++ b/lib/jsdom/living/attributes/NamedNodeMap-impl.js
@@ -14,7 +14,11 @@ exports.implementation = class NamedNodeMapImpl {
   get _attributeList() {
     return this._element._attributeList;
   }
-
+  * [Symbol.iterator]() {
+    for (let i = 0; i < this._attributeList.length; ++i) {
+      yield this._attributeList[i];
+    }
+  }
   get [idlUtils.supportedPropertyIndices]() {
     return this._attributeList.keys();
   }
diff --git a/lib/jsdom/living/css/helpers/computed-style.js b/lib/jsdom/living/css/helpers/computed-style.js
index 6ee5cd43..4c9fd3b3 100644
--- a/lib/jsdom/living/css/helpers/computed-style.js
+++ b/lib/jsdom/living/css/helpers/computed-style.js
@@ -101,28 +101,25 @@ function handleSheet(sheetImpl, elementImpl, declaration, specificities) {
 }
 
 function handleRule(ruleImpl, elementImpl, declaration, specificities) {
-  if (ruleMightMatchElement(ruleImpl, elementImpl)) {
-    const { ast, match } = matches(ruleImpl.selectorText, elementImpl);
-    if (match) {
-      handleStyle(ruleImpl.style, declaration, specificities, ast);
-    }
+  if (ruleMightMatchElement(ruleImpl, elementImpl) && matches(ruleImpl.selectorText, elementImpl)) {
+    handleStyle(ruleImpl.style, declaration, specificities, ruleImpl.selectorText);
   }
 }
 
-function handleStyle(style, declaration, specificities, ast) {
+function handleStyle(style, declaration, specificities, selectorText) {
   for (let i = 0; i < style.length; i++) {
     const property = style.item(i);
-    handleProperty(property, declaration, style, specificities, ast);
+    handleProperty(property, declaration, style, specificities, selectorText);
   }
 }
 
-function handleProperty(property, declaration, style, specificities, ast) {
+function handleProperty(property, declaration, style, specificities, selectorText) {
   const value = style.getPropertyValue(property);
   const priority = style.getPropertyPriority(property);
   if (priority) {
     declaration.setProperty(property, value, priority);
   } else if (!declaration.getPropertyPriority(property)) {
-    const { value: specificity } = Specificity.max(...Specificity.calculate(ast));
+    const { value: specificity } = Specificity.max(...Specificity.calculate(selectorText));
     if (specificities.has(property)) {
       if (Specificity.compare(specificity, specificities.get(property)) >= 0) {
         specificities.set(property, specificity);
@@ -143,55 +140,18 @@ function handlePropertyForInlineStyle(property, declaration, style) {
   }
 }
 
-function ruleMightMatchElement(ruleImpl, elementImpl) {
-  if (!ruleImpl._selectorSubjects) {
-    const domSelector = elementImpl._ownerDocument._getDOMSelector();
-    ruleImpl._selectorSubjects =
-      domSelector.extractSubjects(
-        ruleImpl.selectorText,
-        elementImpl._ownerDocument.contentType !== "text/html"
-      );
-  }
-
-  const subjects = ruleImpl._selectorSubjects;
-  if (subjects.length === 0) {
-    return true;
-  }
-
-  const id = elementImpl.getAttributeNS(null, "id");
-  const { classList } = elementImpl;
-  // TODO: Remove toLowerCase() after updating the dom-selector.
-  const tag = elementImpl._localName.toLowerCase();
-  for (const keys of subjects) {
-    let mightMatch = true;
-    if (keys.id && keys.id !== id) {
-      mightMatch = false;
-    } else if (keys.className && !classList.contains(keys.className)) {
-      mightMatch = false;
-      // TODO: Remove toLowerCase() after updating the dom-selector.
-    } else if (keys.tag && keys.tag.toLowerCase() !== tag) {
-      mightMatch = false;
-    }
-
-    if (mightMatch) {
-      return true;
-    }
-  }
-
-  return false;
+function ruleMightMatchElement() {
+  return true;
 }
 
 function matches(selectorText, elementImpl) {
-  const domSelector = elementImpl._ownerDocument._getDOMSelector();
-  const { ast, match, pseudoElement } = domSelector.check(selectorText, elementImpl);
-  // `pseudoElement` is a pseudo-element selector (e.g. `::before`).
-  // However, we do not support getComputedStyle(element, pseudoElement), so `match` is set to `false`.
-  if (pseudoElement) {
-    return {
-      match: false
-    };
+  const engine = elementImpl._ownerDocument._getSelectorEngine();
+
+  try {
+    return engine.matches(selectorText, elementImpl);
+  } catch {
+    return false;
   }
-  return { ast, match, pseudoElement };
 }
 
 function replaceEmptyValueAndKeywords(
diff --git a/lib/jsdom/living/helpers/by-id-cache.js b/lib/jsdom/living/helpers/by-id-cache.js
index 24b397b5..763ab983 100644
--- a/lib/jsdom/living/helpers/by-id-cache.js
+++ b/lib/jsdom/living/helpers/by-id-cache.js
@@ -64,4 +64,35 @@ module.exports = class ByIdCache {
     this._map.delete(id);
     return null;
   }
+
+  getAll(id) {
+    // Duplicate IDs still rescan the tree. A better cache would keep duplicate
+    // elements directly, but this avoids changing the normal getElementById() path.
+    const value = this._map.get(id);
+    if (!value) {
+      return [];
+    }
+
+    if (value.count === 1 && value.element) {
+      return [value.element];
+    }
+
+    const elements = [];
+    let i = 0;
+
+    for (const descendant of domSymbolTree.treeIterator(this._root)) {
+      if (descendant.nodeType === NODE_TYPE.ELEMENT_NODE && descendant.getAttributeNS(null, "id") === id) {
+        elements[i++] = descendant;
+      }
+    }
+
+    if (i === 0) {
+      this._map.delete(id);
+    } else {
+      value.element = elements[0];
+      value.count = i;
+    }
+
+    return elements;
+  }
 };
diff --git a/lib/jsdom/living/nodes/Document-impl.js b/lib/jsdom/living/nodes/Document-impl.js
index b85d6b0e..116bcb51 100644
--- a/lib/jsdom/living/nodes/Document-impl.js
+++ b/lib/jsdom/living/nodes/Document-impl.js
@@ -1,7 +1,7 @@
 "use strict";
 
 const { CookieJar } = require("tough-cookie");
-const { DOMSelector } = require("@asamuzakjp/dom-selector");
+const { createSelectlet } = require("selectlet");
 
 const NodeImpl = require("./Node-impl").implementation;
 const idlUtils = require("../../../generated/idl/utils");
@@ -116,7 +116,7 @@ const eventInterfaceTable = {
 };
 
 class DocumentImpl extends NodeImpl {
-  #domSelector = null;
+  #selectorEngine = null;
 
   constructor(globalObject, args, privateData) {
     super(globalObject, args, privateData);
@@ -216,19 +216,79 @@ class DocumentImpl extends NodeImpl {
     this._styleCache = new WeakMap();
   }
 
-  // The `DOMSelector` instance is lazily created, as it is somewhat expensive to create and not always needed.
-  _getDOMSelector() {
-    if (!this.#domSelector) {
-      this.#domSelector = new DOMSelector(this._globalObject, this._ownerDocument, {
-        domSymbolTree,
-        idlUtils
+  _getSelectorEngine() {
+    if (!this.#selectorEngine) {
+      function htmlAttrName(el, name) {
+        return (el._namespaceURI === HTML_NS) &&
+          (el._ownerDocument._parsingMode === "html") ?
+          asciiLowercase(name) :
+          name;
+      }
+
+      function attrByName(el, name) {
+        const entry = el._attributesByNameMap.get(htmlAttrName(el, name));
+        return entry ? entry[0] : null;
+      }
+
+      function attrByNameNS(el, namespace, localName) {
+        if (namespace === "") {
+          namespace = null;
+        }
+
+        const list = el._attributeList;
+        for (let i = 0; i < list.length; i++) {
+          const attr = list[i];
+          if (attr._namespace === namespace && attr._localName === localName) {
+            return attr;
+          }
+        }
+
+        return null;
+      }
+
+      this.#selectorEngine = createSelectlet(this, {
+        errors: {
+          syntax: err => DOMException.create(this._globalObject, [err.message, "SyntaxError"])
+        },
+        caps: {
+          doc: {
+            cachedIds: (doc, id) => doc._byIdCache.getAll(id),
+            designMode: doc => {
+              const wrapper = idlUtils.wrapperForImpl(doc);
+              return typeof wrapper?.designMode === "string" ? wrapper.designMode : undefined;
+            }
+          },
+          el: {
+            getId: el => {
+              const entry = el._attributesByNameMap.get("id");
+              return entry ? entry[0]._value : "";
+            },
+
+            getClass: el => {
+              const entry = el._attributesByNameMap.get("class");
+              return entry ? entry[0]._value : "";
+            },
+
+            getLocalName: el => el._localName,
+            getNamespaceURI: el => el._namespaceURI,
+
+            getAttribute: (el, name) => {
+              const attr = attrByName(el, name);
+              return attr ? attr._value : null;
+            },
+
+            getAttributeNS: (el, namespace, localName) => {
+              const attr = attrByNameNS(el, namespace, localName);
+              return attr ? attr._value : null;
+            },
+
+            hasAttribute: (el, name) => attrByName(el, name) !== null,
+            hasAttributeNS: (el, namespace, localName) => attrByNameNS(el, namespace, localName) !== null
+          }
+        }
       });
     }
-    return this.#domSelector;
-  }
-
-  _clearDOMSelector() {
-    this.#domSelector?.clear();
+    return this.#selectorEngine;
   }
 
   _clearBaseURLCache() {
diff --git a/lib/jsdom/living/nodes/Element-impl.js b/lib/jsdom/living/nodes/Element-impl.js
index 0ef1ce89..76e7d644 100644
--- a/lib/jsdom/living/nodes/Element-impl.js
+++ b/lib/jsdom/living/nodes/Element-impl.js
@@ -99,9 +99,6 @@ class ElementImpl extends NodeImpl {
       this._classList.attrModified();
     }
 
-    // Clear domSelector cached results on attribute change.
-    this._ownerDocument._clearDOMSelector();
-
     this._attrModifiedSlotableMixin(name, value, oldValue);
   }
 
@@ -530,13 +527,13 @@ class ElementImpl extends NodeImpl {
   }
 
   closest(selectors) {
-    const domSelector = this._ownerDocument._getDOMSelector();
-    return domSelector.closest(selectors, this);
+    const engine = this._ownerDocument._getSelectorEngine();
+    return engine.closest(selectors, this);
   }
 
   matches(selectors) {
-    const domSelector = this._ownerDocument._getDOMSelector();
-    return domSelector.matches(selectors, this);
+    const engine = this._ownerDocument._getSelectorEngine();
+    return engine.matches(selectors, this);
   }
 
   webkitMatchesSelector(selectors) {
diff --git a/lib/jsdom/living/nodes/ParentNode-impl.js b/lib/jsdom/living/nodes/ParentNode-impl.js
index adfc851d..c0623e89 100644
--- a/lib/jsdom/living/nodes/ParentNode-impl.js
+++ b/lib/jsdom/living/nodes/ParentNode-impl.js
@@ -6,6 +6,8 @@ const { domSymbolTree } = require("../helpers/internal-constants");
 const NODE_TYPE = require("../node-type");
 const { convertNodesIntoNode } = require("../node");
 
+// const idlUtils = require("../../../generated/idl/utils");
+
 class ParentNodeImpl {
   get children() {
     if (!this._childrenList) {
@@ -63,19 +65,19 @@ class ParentNodeImpl {
     if (shouldAlwaysSelectNothing(this)) {
       return null;
     }
-    const domSelector = this._ownerDocument._getDOMSelector();
-    return domSelector.querySelector(selectors, this);
+
+    const engine = this._ownerDocument._getSelectorEngine();
+    return engine.first(selectors, this);
   }
 
   // WARNING FOR INTERNAL USERS:
-  // This returns a NodeList impl, not a NodeList wrapper. NodeList impls are not iterable and do not have indexed
-  // properties. To iterate over them, use `for (let i = 0; i < nodeListImpl.length; ++i) { nodeListImpl.item(i) }`.
+  // This returns a NodeList impl, not an array or wrapper.
   querySelectorAll(selectors) {
     if (shouldAlwaysSelectNothing(this)) {
       return NodeList.createImpl(this._globalObject, [], { nodes: [] });
     }
-    const domSelector = this._ownerDocument._getDOMSelector();
-    const nodes = domSelector.querySelectorAll(selectors, this);
+    const engine = this._ownerDocument._getSelectorEngine();
+    const nodes = engine.select(selectors, this);
     return NodeList.createImpl(this._globalObject, [], { nodes });
   }
 }
diff --git a/package-lock.json b/package-lock.json
index 5383c4ba..ce28de74 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -22,6 +22,7 @@
         "lru-cache": "^11.3.5",
         "parse5": "^8.0.1",
         "saxes": "^6.0.0",
+        "selectlet": "^0.1.0-alpha.1",
         "symbol-tree": "^3.2.4",
         "tough-cookie": "^6.0.1",
         "undici": "^7.25.0",
@@ -2675,6 +2676,12 @@
         "node": ">=v12.22.7"
       }
     },
+    "node_modules/selectlet": {
+      "version": "0.1.0-alpha.1",
+      "resolved": "https://registry.npmjs.org/selectlet/-/selectlet-0.1.0-alpha.1.tgz",
+      "integrity": "sha512-DdEj73l+gJeDxdHATB46wpHOb9Lqe7xKnIC4NedMY4YIxpDNkqlH5h+mueWH2raNdVOHUSlkB/yHxN9lBfB/IA==",
+      "license": "MIT"
+    },
     "node_modules/semver": {
       "version": "7.7.4",
       "resolved": "https://registry.npmjs.org/semver/-/semver-7.7.4.tgz",
diff --git a/package.json b/package.json
index 2c0b1770..4ada0090 100644
--- a/package.json
+++ b/package.json
@@ -36,6 +36,7 @@
     "lru-cache": "^11.3.5",
     "parse5": "^8.0.1",
     "saxes": "^6.0.0",
+    "selectlet": "^0.1.0-alpha.1",
     "symbol-tree": "^3.2.4",
     "tough-cookie": "^6.0.1",
     "undici": "^7.25.0",
