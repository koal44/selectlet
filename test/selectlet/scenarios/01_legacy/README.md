# Legacy selector suites

These tests were ported from NWsAPI's older browser suites.

Much of that coverage was gathered from selector engines of the
jQuery/Slick/Prototype/etc. era. Files are mostly named as found.

The tests may be old, but they are not dead coverage. They run against modern
browsers and remain valuable as a broad selector corpus for `selectlet`.
